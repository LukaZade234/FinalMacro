import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import gui 1.0
import "../components"

/*
    Advisor › Lists — one-click `$dl` / `$adl` / `$wl` switching.

    Deliberately blank: the previous draft was cleared so this page can be
    designed from scratch.
*/
Item {
    id: root
    clip: true
}
